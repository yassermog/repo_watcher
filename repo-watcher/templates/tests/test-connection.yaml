apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "repo-watcher.fullname" . }}-test-connection"
  labels:
    {{- include "repo-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "repo-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
